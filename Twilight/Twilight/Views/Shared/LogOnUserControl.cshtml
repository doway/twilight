@using System.Web.Mvc;
@using System.Web.Mvc.Html;
    @if (Request.IsAuthenticated) {
        @Html.ActionLink("Logout" + string.Format(" ({0}) ", Page.User.Identity.Name), "LogOff", "Account")
    }
    else {
         @Html.ActionLink("Log On", "LogOn", "Account")
    }